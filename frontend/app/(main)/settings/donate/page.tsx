import { PlaceholderSettingsPage } from "@/components/settings/PlaceholderSettingsPage";

export default function DonateSettingsPage() {
  return (
    <PlaceholderSettingsPage
      title="Donate to Beacon"
      description="Beacon is a demo project — no donations are collected."
    />
  );
}
